// Tailwind 4 uses CSS-first config via @theme in globals.css.
// This file is a placeholder — design tokens live in src/styles/globals.css.
// Phase-02 will extend this if JS-side config is needed.
export default {};
